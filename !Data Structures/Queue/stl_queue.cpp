#include <iostream>
#include <queue>

using namespace std;

int main(){
  queue<int> q;        // Creates an empty queue O(1)

  // Functions:
  
  // q.push(value);            // Adds 'value' to the back of the queue O(1)
  // q.pop();                  // Removes the front element O(1)
  // q.front();                // Returns the front element O(1)
  // q.back();                 // Returns the last element O(1)
  // q.empty();                // Returns true if the queue is empty O(1)
  // q.size();                 // Returns the number of elements in the queue O(1)
}