#include <iostream>
#include <map>

using namespace std;

int main(){
  // mm.insert({key, value});       // Inserts a key-value pair (duplicates allowed) O(log n)
  // mm.erase(key);                 // Removes all elements with the given key O(log n)
  // mm.erase(iter);                // Removes the element at the iterator's position O(1)
  // mm.clear();                    // Clears all elements from the multimap O(n)
  // mm.find(key);                  // Returns iterator to the first occurrence of 'key' or mm.end() O(log n)
  // mm.count(key);                 // Returns the number of occurrences of 'key' O(log n)
  // mm.contains(key);              // (C++20) Returns true if the key exists O(log n)
  // mm.begin();                    // Returns an iterator to the first element O(1)
  // mm.end();                      // Returns an iterator to the past-the-last element O(1)
  // mm.lower_bound(key);           // Returns iterator to the first element >= key O(log n)
  // mm.upper_bound(key);           // Returns iterator to the first element > key O(log n)
  // mm.equal_range(key);           // Returns {lower_bound, upper_bound} O(log n)

}