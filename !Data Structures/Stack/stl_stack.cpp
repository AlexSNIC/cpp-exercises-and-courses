#include <iostream>
#include <stack>

using namespace std;

int main(){
  stack<int> s;

  // Functions:

  // s.push(value);             // Pushes 'value' onto the top of the stack O(1)
  // s.pop();                   // Removes the top element O(1)
  // s.top();                   // Returns the top element O(1)
  // s.empty();                 // Returns true if the stack is empty O(1)
  // s.size();                  // Returns the number of elements in the stack O(1)
}