#include <iostream>
#include <vector>

using namespace std;

int main() {
  // v.push_back(value);   // Adds 'value' to the end of the vector
  // v.pop_back();         // Removes the last element
  // v.clear();            // Clears all elements from the vector
  // v.insert(iter, value); // Inserts 'value' at the specified position
  // v.erase(iter);         // Removes the element at the specified iterator position
  // v.begin();            // Returns an iterator to the first element
  // v.end();              // Returns an iterator to the past-the-last element
  // v.size();             // Returns the number of elements in the vector
  // v.empty();            // Returns true if the vector is empty
  // v.at(index);          // Returns the element at the specified index (throws out_of_range if invalid)
  // v.front();            // Returns the first element
  // v.back();             // Returns the last element
  // v.resize(new_size);   // Resizes the vector to the specified size (truncates or fills with default values)
  // v.reserve(capacity);  // Reserves memory for at least 'capacity' elements
}